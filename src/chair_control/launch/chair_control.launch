<launch>

  <!-- Load joint controller configurations from YAML file to parameter server -->
  <rosparam file="$(find chair_control)/config/chair_control.yaml" command="load"/>

  <!-- load the controllers -->
  <node name="chair_controller_spawner"
    pkg="controller_manager"
    type="spawner" respawn="false"
    output="screen" ns="/chair"
    args="joint_state_controller
      rightWheel_effort_controller
      leftWheel_effort_controller"/>


  <!-- convert joint states to TF transforms for rviz, etc -->
  <node name="robot_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher" respawn="false" output="screen">
    <param name="robot_description" textfile="$(find chair_description)/urdf/chair.urdf"/>
    <remap from="/joint_states" to="/chair/joint_states" />
  </node>

</launch>
